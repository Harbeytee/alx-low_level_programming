#include <stdio.h>
/* main function - prints hexadecimal*/
int main(void)
{
	char n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(c);
	}

	for (n = 'a'; n <= 'f'; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}